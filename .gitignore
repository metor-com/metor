.DS_Store
# local, per-developer files (never commit)
CLAUDE.local.md
.claude/settings.local.json
.env
.env.*
!.env.example
*.log
# build output and dependencies
node_modules/
frontend/dist/
# editors
.idea/
.vscode/
client/desktop/ui/
client/desktop/dist/
client/desktop/resources/
client/mobile/www/

# Firebase configuration of a build (client/mobile): identifies the publisher's project – never in the repository
client/mobile/android/app/google-services.json
# Firebase service account keys (the relay's secret) – never in the repository, wherever they are dropped
*firebase-adminsdk*.json
.claude/launch.json

# Local development build fingerprints
.metor-dev/
